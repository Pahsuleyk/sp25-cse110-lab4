1. Line 12 should print `3` as it calls the console to print `i` after the for loop, so with `i` being the iterator for the for loop, it increments by one until it hits the end of the list and thus results in `i` being the size of the list. With line 19 using the function with the list `[100, 200, 300]`, the list size will be 3.

2.